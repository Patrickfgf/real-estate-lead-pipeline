# Configuração do pytest.
# `pythonpath = .` garante que `import src` funcione rodando `pytest` a partir da
# raiz do projeto (sem depender de `python -m pytest`) — inclusive no CI.
[pytest]
pythonpath = .
testpaths = tests
